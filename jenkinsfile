pipeline {
    agent any

    stages {
        stage("print") {
            steps {
                echo 'hello test'
            }
        }
stage("build") {
steps {
		  sh 'gradlew clean build'
		}
}
    }
}
