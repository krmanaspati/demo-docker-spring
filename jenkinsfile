pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo 'hello test'
            }
        }
stage("build") {
		  sh 'gradlew clean build'
		}
    }
}
