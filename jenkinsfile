pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo 'hello test'
            }
        }
stage("build") {
steps {
		  sh 'gradlew clean build'
		}
}
    }
}
